# 模块缺口清单 V1

> 对照《第一阶段（必须有）》《API设计文档 V1》《前端页面-API映射表 V1》与 Monorepo 实现。  
> 更新日期：2026-06-06

## 图例

| 状态 | 含义 |
|------|------|
| ✅ | 已实现 / 已修复 |
| 🟡 | 部分实现 |
| ❌ | 未实现 |
| 📋 | 文档已定义，待开发 |

---

## 一、后端业务包

### 1. `@acs/db` + seed

| 项 | 状态 | 说明 |
|----|------|------|
| Prisma Schema / migrate | ✅ | — |
| Seed admin/operator/reviewer | ✅ | — |
| ANALYST 角色种子 | ❌ | 权限文档有枚举，种子未加 |
| MaterialAsset / Integration 表 | 📋 | P1 素材库 + 外部接入 |
| SystemSetting / ApiKey 表 | 📋 | P1/P2 |
| 测试库 `test.db` + `db:test:*` | ✅ | 集成/包测使用 |

### 2. `@acs/core`

| 项 | 状态 | 说明 |
|----|------|------|
| ApiResponse / AppError / RBAC | ✅ | — |
| 单元测试 | ✅ | `rbac.test.ts`、`api-response.test.ts` |

### 3. `@acs/content-center`

| 项 | 状态 | 说明 |
|----|------|------|
| Topics / Contents CRUD | ✅ | — |
| updateTopic / deleteTopic RBAC | ✅ | OPERATOR 仅操作自己的选题 |
| createContent requireRoles | ✅ | ADMIN / OPERATOR |
| GET /versions/:versionId | ✅ | 已在 `accounts.ts` 暴露 |
| MaterialAsset 独立库 | 📋 | P1 新包或扩展现有 materials |
| 状态机单测 | ✅ | 主要转移已覆盖 |
| topics 单测 | ✅ | 过滤、删除、RBAC |

### 4. `@acs/ai-runtime`

| 项 | 状态 | 说明 |
|----|------|------|
| Prompt / runAgent / orchestrate | ✅ | Mock 默认 |
| POST /agents/body/run | 📋 | P1；编排内已有 BODY |
| TAG 独立 HTTP | ❌ | 仅编排内调用 |
| orchestrate steps 可选 | ❌ | — |
| parsers / task-engine 单测 | ✅ | — |

### 5. `@acs/ima-provider`

| 项 | 状态 | 说明 |
|----|------|------|
| Mock searchAndLog | ✅ | 单测覆盖写库 |
| GET /ima/knowledge-bases/:id | 📋 | 可选；当前前端 client find |

### 6. `@acs/review-center`

| 项 | 状态 | 说明 |
|----|------|------|
| 审核 / 发布任务 | ✅ | — |
| listReviews status 筛选 | ✅ | — |
| listPublishingTasks status 筛选 | ✅ | — |
| GET /reviews/stats | 📋 | P1 |
| GET /publishing/summary | 📋 | P1 |
| 单测 | ✅ | approve、publish 前置、列表筛选 |

### 7. `@acs/account-profile`

| 项 | 状态 | 说明 |
|----|------|------|
| 列表 OPERATOR 过滤 | ✅ | — |
| PATCH /accounts/:id | ✅ | ADMIN / OPERATOR |
| POST /accounts 手动创建 | 📋 | P1 |
| 画像完整字段 UI | 🟡 | 前端仅部分字段 |
| accounts/sync RBAC | ✅ | ADMIN / OPERATOR |

### 8. `@acs/analytics-center`

| 项 | 状态 | 说明 |
|----|------|------|
| Dashboard stats（扁平 6 字段） | ✅ | dashboard 单测 |
| Dashboard alerts / publishedWeek | 📋 | V1.1 |
| GET /analytics/overview | 📋 | P1 |
| 报告 / sync | ✅ | — |

### 9. `@acs/turbopush-adapter`

| 项 | 状态 | 说明 |
|----|------|------|
| Mock 同步/发布 | ✅ | mock 契约单测 |
| 真实 TurboPush | ❌ | P3 |
| 草稿同步 / 发布包 | ❌ | P3；发布页 mock 区块 |

### 10. Integration（新包，P1）

| 项 | 状态 | 说明 |
|----|------|------|
| API Key CRUD | 📋 | `/integration/api-keys` |
| Webhook 配置 | 📋 | `/integration/webhooks` |
| Access logs | 📋 | `/integration/access-logs` |
| External API 鉴权中间件 | 📋 | `X-API-Key` |

---

## 二、API 层（`apps/api`）

### 已实现（~58 端点）

Auth、Topics、Contents、Versions(get+patch)、Agents( title/rewrite/review )、Agent Runs(get list+detail)、Prompts、IMA、Reviews、Publishing、Analytics、Dashboard、Materials(content-bound)

### 文档已定义、代码未实现

| 模块 | 缺失端点 | 优先级 |
|------|----------|--------|
| Material Library | `GET/POST /materials/library/*`, upload | P1 |
| Integration | `/integration/*` | P1 |
| Agents | `POST /agents/body/run` | P1 |
| Accounts | `POST /accounts` | P1 |
| Reviews | `GET /reviews/stats` | P1 |
| Publishing | `GET /publishing/summary` | P1 |
| Analytics | `GET /analytics/overview` | P1 |
| Settings | `GET /settings`, `PUT /settings/:key` | P2 |
| Users | `GET/POST /users`, `GET/PATCH /users/:id` | P2 |
| Auth | `POST /auth/logout` | P2 可选 |
| Versions | `DELETE /versions/:versionId` | 待定 |
| Dashboard | `alerts[]`, `publishedWeek` | V1.1 |

### 已修正（原清单误报为缺失）

| 端点 | 状态 |
|------|------|
| `GET /agent-runs/:id` | ✅ |
| `PATCH /accounts/:id` | ✅ |
| `GET /versions/:versionId` | ✅ |

### 测试

| 项 | 状态 | 说明 |
|----|------|------|
| Fastify `inject` 集成冒烟 | ✅ | `smoke.test.ts` |
| smoke 中 `publishedWeek` 类型 | 🟡 | 与实现 `publishedTotal` 不一致，待实现 V1.1 或改测试 |
| 根目录 `pnpm test` | ✅ | — |

---

## 三、前端（`apps/web`）

> 详见 [前端页面-API映射表 V1](./前端页面-API映射表%20V1.md)

| 页面 | 后端 | 前端 | 缺口 |
|------|------|------|------|
| 登录 | ✅ | ✅ | — |
| 工作台 | 🟡 | 🟡 | 告警 mock；delta 伪随机 |
| 内容项目 | ✅ | 🟡 | 搜索/新建未接；详情 PATCH 未保存 |
| 审核中心 | ✅ | 🟡 | Tab/统计 mock；列表已接 API |
| Agent 任务 | ✅ | ❌ mock | **P0 接线** `GET /agent-runs` |
| 平台账号 | 🟡 | 🟡 | 创建账号未接；卡片字段 mock |
| 知识库配置 | ✅ | ✅ | — |
| 素材库 | 📋 | ❌ mock | P1 等新 API |
| 发布管理 | ✅ | ❌ mock | **P0 接线** `/publishing/tasks` |
| 数据复盘 | 🟡 | ❌ mock | **P0 接线** `/analytics/*` |
| 系统设置 | 📋 | ❌ mock | P1 `/integration/*` |
| 选题 / Prompt | ✅ | 🟡 | 选题 CRUD 部分；Prompt 只读 |
| AI 生成 | 🟡 | ❌ mock | 可复用 agents/ima API |
| 全局顶栏 | ✅ | ✅ | `/auth/me` 已接 |

---

## 四、横切

| 规则 | 状态 |
|------|------|
| REVIEWER 不能审自己内容 | ✅ |
| OPERATOR 数据隔离 | ✅ |
| ANALYST 只看板 | ❌ |
| Prompt 仅 ADMIN 写 | ✅ API |
| 发布需 APPROVED | ✅ |
| 前端按角色隐藏菜单 | ❌ |
| OpenAPI 与实现同步 | 🟡 | 2026-06-06 已刷新 |

---

## 五、优先级 backlog

**P0 — 前端接线已有 API（文档已完成，待开发）**

- [ ] `/agent-tasks` → `GET /agent-runs`, `GET /agent-runs/:id`
- [ ] `/publishing` → `/publishing/tasks/*`
- [ ] `/analytics` → `/analytics/reports`, `/analytics/contents/:id`
- [ ] `/reviews` Tab → `GET /reviews?status=`
- [ ] `/contents/[id]` → `PATCH /contents/:id`, `PATCH /versions/:versionId`

**P1 — 新后端 + 前端**

- [ ] 独立素材库 `MaterialAsset` + `/materials/library/*`
- [ ] 外部 Agent 接入 `/integration/*`
- [ ] `POST /accounts`, `POST /agents/body/run`
- [ ] `GET /reviews/stats`, `GET /publishing/summary`, `GET /analytics/overview`
- [ ] 内容详情编辑、审核驳回 comment 弹窗

**P2 — 系统管理**

- [ ] Users / Settings 模块（`/users`, `/settings`）
- [ ] Prompt CRUD UI
- [ ] Dashboard `alerts[]`, `publishedWeek`

**P3 — 外部集成**

- [ ] 真实 TurboPush / IMA / DeepSeek
- [ ] 发布页草稿同步、小红书/抖音发布包

**已完成（历史 P0）**

- [x] `listPublishingTasks` status 筛选
- [x] topic/content RBAC、`accounts/sync` RBAC
- [x] 前端：内容详情、提交审核
- [x] 测试基础设施 + 包单测 + API 集成冒烟
- [x] API 文档补全（OpenAPI、映射表、Material/Integration/Settings 设计）
