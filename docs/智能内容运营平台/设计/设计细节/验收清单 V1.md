# 验收清单 V1（手工）

> 与自动化测试互补。环境：`pnpm db:setup` 后 `pnpm dev:studio`。  
> 账号见 seed：`admin@acs.local` / `admin123`，`operator@acs.local` / `operator123`，`reviewer@acs.local` / `reviewer123`。

## 自动化（CI 本地）

```bash
pnpm db:test:setup
pnpm test
```

预期：包级单测 + `apps/api` 集成冒烟全部通过。

---

## Sprint 1 — 登录与基础数据

| # | 角色 | 步骤 | 预期 |
|---|------|------|------|
| 1.1 | — | 打开 `/login`，错误密码登录 | 提示失败 |
| 1.2 | admin | 正确登录 | 跳转工作台 |
| 1.3 | admin | 刷新任意业务页 | 保持登录（token 有效） |
| 1.4 | admin | 清除 localStorage token 后访问 `/dashboard` | 跳转登录 |

---

## Sprint 2 — 内容生产

| # | 角色 | 步骤 | 预期 |
|---|------|------|------|
| 2.1 | operator | `/topics` 新建选题 | 列表出现 |
| 2.2 | operator | `/contents` 新建内容 | 列表出现 |
| 2.3 | operator | 选中内容「一键生成」 | 状态变为待审核相关 |
| 2.4 | operator | 进入内容「详情」→「提交审核」 | 成功提示；审核中心有待审 |
| 2.5 | operator | `/ai-generate` 执行 IMA / 标题 Agent | 有 JSON 结果 |

---

## Sprint 3 — 审核与发布

| # | 角色 | 步骤 | 预期 |
|---|------|------|------|
| 3.1 | reviewer | `/reviews` 通过一条待审 | 状态已通过 |
| 3.2 | operator | 对自己创建的内容尝试通过（若 UI 暴露） | 应失败（API 40300） |
| 3.3 | admin | `/publishing` 未审核 versionId 创建任务 | 页面提示需 APPROVED |
| 3.4 | admin | 使用已通过版本创建并「发布」 | 任务 SUCCESS |
| 3.5 | admin | `/dashboard` | 指标卡有数字 |

---

## Sprint 4 — 账号与复盘

| # | 角色 | 步骤 | 预期 |
|---|------|------|------|
| 4.1 | operator | `/accounts` 同步账号 | 列表有 mock 账号 |
| 4.2 | operator | 保存账号画像（定位/风格） | 保存成功 |
| 4.3 | admin | `/analytics` 生成报告 | 有报告数据 |
| 4.4 | admin | `/prompts` 只读列表 | 有 seed Prompt |

---

## RBAC 抽检（API 或 UI）

| # | 步骤 | 预期 |
|---|------|------|
| R1 | reviewer `POST /api/prompts` | code `40300` |
| R2 | operator 不能修改他人选题（API） | `40300` |
| R3 | 发布任务 version 非 APPROVED | `40701` 或前端提示 |

---

## 已知未验收项（P1+）

- 设置页、用户管理
- Prompt 新建/编辑（ADMIN UI）
- ANALYST 角色端到端
- 真实 TurboPush / IMA / DeepSeek
